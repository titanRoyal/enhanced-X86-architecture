###### execute using docker

run "docker build -t[projet name] [path]"
run "docker run -it [project name]"


###### execute normally

change directory to the "UnitTest/vm"

run "node index [../examples/[any file in the folder]]

warning if you execute without docker you must have nodejs installed on your machine